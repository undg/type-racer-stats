export interface Stats {
    id: number
    wpm: number
    accuracy?: number
    points?: number
    rank?: number
    racers?: number
    unixTime?: number
    date?: string
    ghostUrl?: string
    replayUrl?: string
}

